package ru.shariktlt.proxy.iterators;

import ru.shariktlt.proxy.ServerRecord;
import ru.shariktlt.proxy.ServersRegistry;

public class SingleProxyIterator implements ProxyIterator{
    public SingleProxyIterator(ServersRegistry serversRegistry, ServerRecord key) {

    }
}
