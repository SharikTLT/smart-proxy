package ru.shariktlt.proxy.iterators;

import ru.shariktlt.proxy.ServerRecord;
import ru.shariktlt.proxy.ServersRegistry;

import java.util.List;

public class ListProxyIterator implements ProxyIterator {
    public ListProxyIterator(ServersRegistry serversRegistry, List<ServerRecord> collect) {

    }
}
